global:
  scrape_interval: 10s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:9464"]

  # We’ll add these once services exist:
  # - job_name: auth-service
  #   static_configs:
  #     - targets: ["auth-service:8080"]
